import "./heading-title.css";

const HeadingTitle = ({ title }) => {
    return ( 
        <div className="heading-title">
            <h2 className="title-heading-title">{title}</h2>
        </div>
     );
}
 
export default HeadingTitle;